include: ../analysis_options.yaml

analyzer:
  errors:
    # These are command-line probes: what they print is the whole point of
    # them, and none of it ships inside the app.
    avoid_print: ignore
